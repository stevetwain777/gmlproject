/*var aabb = argument0
var point = argument1

// Get the vector from center of rect to point
var aabb_width = aabb_width(aabb)
var aabb_height = aabb_height(aabb)

var aabb_center = vec2(aabb[AABB_L] + (width / 2.0), aabb[AABB_T] + (height / 2.0))
var dir_vec = vec2_sub(point, aabb_center)

var dir_vec = vec2_major(dir_vec)

*/